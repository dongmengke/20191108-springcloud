#从github上读取配置文件名
spring.cloud.config.name=application-dev
#配置profiles的名称
spring.cloud.config.profile=dev
#配置label(如果使用的默认的，就不需要配置（master）)
spring.cloud.config.label=master
#配置config的服务地址
spring.cloud.config.uri=http://localhost:4081